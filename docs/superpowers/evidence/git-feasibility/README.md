# Git Feasibility Evidence Layout

This directory stores archived output from the signed `GitFeasibilityHarness` matrix. Evidence is generated by `script/git_evidence_feasibility_run.sh` and must not be hand-edited.

## Directory Contract

Each allowlisted machine tuple gets one subdirectory:

```text
docs/superpowers/evidence/git-feasibility/
  <os-build-family>-<architecture>/
    manifest.json
    scenarios/
      descriptor_transfer.json
      sandbox_denial.json
      git_transition.json
      cleanup.json
    logs/
      <scenario-id>.log            (optional; populated by later tasks)
    snapshots/
      <scenario-id>/                 (optional; populated by later tasks)
```

`<os-build-family>` matches `manifest.json` → `osBuildFamily` (for example `macOS-14.5-23F79`).
`<architecture>` matches `manifest.json` → `architecture` (for example `arm64` or `x86_64`).

## `manifest.json` Schema

| Field | Type | Description |
| --- | --- | --- |
| `schemaVersion` | number | Manifest schema version. Task 1 uses `1`. |
| `pearcleanerVersion` | string | Pearcleaner marketing version from the harness bundle. |
| `harnessVersion` | string | Harness bundle marketing version (same source as Pearcleaner in Task 1). |
| `runnerVersion` | string | Signed `GitRunner` version string used for tuple gating. |
| `appleGitVersion` | string | Output of `/usr/bin/git --version` on the test host. |
| `osBuildFamily` | string | macOS product version + build family identifier. |
| `architecture` | string | Machine architecture at test time. |
| `testTimestamp` | string (ISO-8601) | UTC timestamp when the harness finished. |
| `overallStatus` | string | `passed` or `failed`. |
| `scenarios` | array | Per-scenario structured results (see below). |

### Scenario Result Object

| Field | Type | Description |
| --- | --- | --- |
| `id` | string | One of `descriptor_transfer`, `sandbox_denial`, `git_transition`, `cleanup`. |
| `status` | string | `passed`, `failed`, or `not_implemented`. |
| `passed` | boolean | Whether the scenario succeeded. |
| `sandboxLogPath` | string or null | Relative path under the tuple directory to a sandbox denial log. |
| `filesystemSnapshotPath` | string or null | Relative path under the tuple directory to a filesystem snapshot. |
| `details` | object | Structured key/value metadata for the scenario run. |

Task 1 placeholder scenarios write `status: "not_implemented"` and `passed: false`. The harness exits non-zero until later tasks implement the matrix.

## Generation

Run from the repository root:

```bash
script/git_evidence_feasibility_run.sh
```

The script:

1. Builds `GitFeasibilityHarness Release`.
2. Rejects ad-hoc or unsigned Release bundles.
3. Runs the harness with `GIT_FEASIBILITY_OUTPUT` pointed at a staging directory.
4. Archives the staged evidence into this directory using the `<os-build-family>-<architecture>` tuple name from `manifest.json`.

## Consumer Contract (Task 2+)

`GitFeasibilityRegistry` loads `manifest.json` for the current host tuple and requires an exact match on:

- `osBuildFamily`
- `architecture`
- `pearcleanerVersion`
- `runnerVersion`
- `appleGitVersion`

Any mismatch or missing manifest disables Git evidence collection in production builds.

## Host requirements (Task 11 matrix)

The Release feasibility matrix requires a **Developer ID–signed and notarized** `GitFeasibilityHarness.app`. On hosts where embedded `GitEvidenceService.xpc` or bundled `GitRunner` cannot launch (for example unnotarized Developer ID builds rejected by Gatekeeper), XPC scenarios remain failed until notarization completes.

The harness orchestrator itself is intentionally **not** App Sandbox–restricted so it can archive evidence and drive embedded XPC services. `GitEvidenceService` and `GitRunner` retain restrictive sandbox entitlements under test.

Document deferred architectures in the feasibility script output when Rosetta or notarization is unavailable.

## CI verification gates (slice 2 Task 12)

GitHub Actions runs `script/test_secret_detector_corpus.sh`, `script/test_git_evidence_boundaries.sh`, and `script/git_evidence_sandbox_checks.sh` with `GIT_FEASIBILITY_SANDBOX_GATE=deferred` until notarized harness evidence produces `overallStatus: passed` manifests. The signed harness matrix step runs only when the runner has a **Developer ID Application** signing identity; otherwise CI documents the deferral and relies on checked-in archived evidence for registry layout validation.
